class Scrapper {
    scrap(siteData){

        if(!siteData){
            throw new Error("Input parameter \"siteData\" is required!");
        }

        try {
            
        } catch (error) {
            throw error;
        }
    }
}